import styled from "styled-components";

export const Container = styled.span`
    display: flex;
    align-items: center;
    gap: 6px;
    font-weight: bold;
    cursor: pointer;

    width: 3rem;

    > img {
        height: 22px;
    }
`